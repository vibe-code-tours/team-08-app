---
phase: quick-260718-60d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "CLAUDE.md accurately states the desktop gate covers only finger-selection and roulette phases"
    - "The stale 'finger-selection through next-round' range is removed"
  artifacts:
    - CLAUDE.md
  key_links:
    - "CLAUDE.md bullet matches TOUCH_REQUIRED_PHASES in src/App.tsx (source of truth)"
---

<objective>
Fix stale documentation in CLAUDE.md's "Key technical learnings" section. The desktop gate bullet claims the gate "fires only on touch-required game phases (finger-selection through next-round)", but the actual `TOUCH_REQUIRED_PHASES` array in `src/App.tsx` was narrowed to `['finger-selection', 'roulette']` (commit 838d8ef). Update the bullet to describe current behavior accurately.

Purpose: Prevent future sessions from relearning or acting on incorrect documented behavior. The doc is the "don't relearn these" reference — stale entries here actively mislead.
Output: Corrected bullet in CLAUDE.md (lines 17-19).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct desktop gate bullet in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>In the "Key technical learnings (don't relearn these)" section, update the desktop gate bullet (currently lines 17-19). Replace the phrase describing the gate as firing on "finger-selection through next-round" with the accurate range: the gate fires only on the finger-selection and roulette phases — the only screens that require touch input (finger-selection through roulette drive `useMultiTouch`). Note that the remaining phases (player-selected, truth-dare-choice, card-reveal, voting, result, next-round) are onClick-driven and work fine with a mouse, so the gate does not cover them. Preserve the rest of the bullet: it still never fires on intro/setup, and still offers a session-scoped "Continue anyway" escape hatch. Source of truth is `TOUCH_REQUIRED_PHASES` in src/App.tsx line 21, which is `['finger-selection', 'roulette']`. Pure prose edit — no source code changes.</action>
  <verify>
    <automated>grep -q "finger-selection and roulette" CLAUDE.md && ! grep -q "finger-selection through next-round" CLAUDE.md && echo PASS</automated>
  </verify>
  <done>CLAUDE.md's desktop gate bullet describes coverage as finger-selection and roulette only; the "finger-selection through next-round" phrasing is gone; the intro/setup exclusion and "Continue anyway" escape hatch remain documented.</done>
</task>

</tasks>

<verification>
- CLAUDE.md bullet matches the `TOUCH_REQUIRED_PHASES = ['finger-selection', 'roulette']` array in src/App.tsx.
- No source code files were modified (documentation-only change).
</verification>

<success_criteria>
The desktop gate bullet in CLAUDE.md accurately reflects that the gate covers only the finger-selection and roulette phases, matching the current source of truth in src/App.tsx.
</success_criteria>

<output>
Create `.planning/quick/260718-60d-fix-stale-claude-md-documentation-of-des/260718-60d-SUMMARY.md` when done.
</output>
