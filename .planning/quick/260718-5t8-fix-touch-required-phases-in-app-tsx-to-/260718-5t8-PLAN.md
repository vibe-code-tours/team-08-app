---
phase: quick-260718-5t8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Desktop/mouse users reach player-selected, truth-dare-choice, card-reveal, voting, result, and next-round screens without the QR handoff gate."
    - "Desktop/mouse users are still gated at finger-selection and roulette (the only touch-required phases)."
  artifacts:
    - src/App.tsx
  key_links:
    - "TOUCH_REQUIRED_PHASES feeds showGate in ActiveScreen — narrowing the array narrows the gate."
---

<objective>
Narrow `TOUCH_REQUIRED_PHASES` in `src/App.tsx` from 8 phases to only the two that actually require touch input: `finger-selection` and `roulette`.

Purpose: The six non-touch phases (player-selected, truth-dare-choice, card-reveal, voting, result, next-round) are driven entirely by `onClick` handlers and work fine with a mouse. Gating them behind the desktop QR handoff screen blocks desktop/mouse users from screens they can use. Code review of PR #82 confirmed none of those screens use `useMultiTouch` or touch events.
Output: Updated `src/App.tsx` with a 2-element `TOUCH_REQUIRED_PHASES` array.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/App.tsx
@src/App.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow TOUCH_REQUIRED_PHASES to touch-only phases</name>
  <files>src/App.tsx</files>
  <action>Replace the `TOUCH_REQUIRED_PHASES` array literal (currently listing eight phases: finger-selection, roulette, player-selected, truth-dare-choice, card-reveal, voting, result, next-round) with a two-element array containing only `'finger-selection'` and `'roulette'`. Keep the `const TOUCH_REQUIRED_PHASES: GamePhase[]` declaration and its doc comment intact. Do not touch `ScreenContent`, `ActiveScreen`, `showGate` logic, or any other code — the gate mechanism already reads from this array; narrowing the array is the only change needed.</action>
  <verify>
    <automated>npm run lint && npm run test && npm run build</automated>
  </verify>
  <done>`TOUCH_REQUIRED_PHASES` contains exactly `['finger-selection', 'roulette']`. Lint, test, and build all pass. `grep -c "player-selected\|truth-dare-choice\|card-reveal\|voting\|result\|next-round" src/App.tsx` still finds those strings only in the `ScreenContent` switch cases, not in the `TOUCH_REQUIRED_PHASES` array.</done>
</task>

</tasks>

<verification>
- `npm run lint && npm run test && npm run build` all pass.
- `TOUCH_REQUIRED_PHASES` array has exactly 2 entries: `finger-selection` and `roulette`.
- `ActiveScreen`, `ScreenContent`, and `showGate` logic are unchanged apart from the array they consume.
</verification>

<success_criteria>
Desktop/non-touch users encounter the QR handoff gate only on `finger-selection` and `roulette` phases. All other phases render their screens normally on desktop. Full lint/test/build suite is green.
</success_criteria>

<output>
Create `.planning/quick/260718-5t8-fix-touch-required-phases-in-app-tsx-to-/260718-5t8-SUMMARY.md` when done.
</output>
