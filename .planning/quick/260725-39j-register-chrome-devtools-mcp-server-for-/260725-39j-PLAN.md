---
phase: quick
plan: 260725-39j
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp.json
  - .claude/settings.local.json
autonomous: true
requirements: [QUICK-260725-39j]
must_haves:
  truths:
    - "Claude Code launches the chrome-devtools MCP server automatically at session start"
    - "No per-session manual approval prompt appears for the chrome-devtools server"
  artifacts:
    - ".mcp.json declaring the chrome-devtools stdio server"
    - ".claude/settings.local.json with enabledMcpjsonServers listing chrome-devtools"
  key_links:
    - "The server key in .mcp.json (chrome-devtools) matches the entry in enabledMcpjsonServers"
---

<objective>
Register the `chrome-devtools-mcp` server for this project so Claude Code auto-launches it at session start without a manual per-session approval prompt.

Purpose: Enable Chrome DevTools MCP tooling for browser/debug automation in this project.
Output: A project-level `.mcp.json` and an updated `.claude/settings.local.json` that pre-approves the server.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Config-only change. No source code, tests, or build changes.

Reference pattern (from sibling project `applytrail`, mirror this exactly):
```json
{
  "mcpServers": {
    "chrome-devtools": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"],
      "env": {}
    }
  }
}
```

Current `.claude/settings.local.json` contains only a `permissions.allow` array — no `enabledMcpjsonServers` key yet. The npm package and Chrome profile cache (`~/.cache/chrome-devtools-mcp`) already exist on disk, so no install step is needed.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project .mcp.json declaring the chrome-devtools server</name>
  <files>.mcp.json</files>
  <action>Create `.mcp.json` at the project root with an `mcpServers` object containing a single `chrome-devtools` entry. Mirror the applytrail pattern exactly: `type` = "stdio", `command` = "npx", `args` = ["-y", "chrome-devtools-mcp@latest"], `env` = {}. Do NOT add any other servers. This file did not previously exist, so create it fresh.</action>
  <verify>node -e "const c=require('./.mcp.json'); if(c.mcpServers['chrome-devtools'].command!=='npx'||c.mcpServers['chrome-devtools'].args.join(' ')!=='-y chrome-devtools-mcp@latest'){process.exit(1)}"</verify>
  <done>`.mcp.json` exists at project root, is valid JSON, and declares the `chrome-devtools` stdio server pointing at `npx -y chrome-devtools-mcp@latest`.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-approve the server in settings.local.json</name>
  <files>.claude/settings.local.json</files>
  <action>Edit `.claude/settings.local.json` to add a top-level `enabledMcpjsonServers` array containing the single string `"chrome-devtools"` (this key name must match the server key in `.mcp.json`). Preserve the existing `permissions.allow` array unchanged — add the new key alongside it, do not overwrite or remove any existing entries. This pre-approval is what suppresses the per-session "approve this MCP server?" prompt.</action>
  <verify>node -e "const c=require('./.claude/settings.local.json'); if(!c.enabledMcpjsonServers.includes('chrome-devtools')){process.exit(1)} if(!c.permissions.allow.includes('Bash(gh pr *)')){process.exit(1)}"</verify>
  <done>`.claude/settings.local.json` is valid JSON, includes `enabledMcpjsonServers: ["chrome-devtools"]`, and retains all pre-existing `permissions.allow` entries.</done>
</task>

</tasks>

<verification>
- `.mcp.json` parses as valid JSON and declares the `chrome-devtools` stdio server.
- `.claude/settings.local.json` parses as valid JSON, lists `chrome-devtools` under `enabledMcpjsonServers`, and preserves the original `permissions.allow` array.
- The server key in `.mcp.json` (`chrome-devtools`) matches the string in `enabledMcpjsonServers`.
</verification>

<success_criteria>
On next Claude Code session start in this project, the chrome-devtools MCP server launches automatically with no manual approval prompt.
</success_criteria>

<output>
Create `.planning/quick/260725-39j-register-chrome-devtools-mcp-server-for-/260725-39j-SUMMARY.md` when done
</output>
