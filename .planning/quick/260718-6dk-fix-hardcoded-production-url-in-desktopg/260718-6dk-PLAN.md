---
phase: 260718-6dk-fix-hardcoded-production-url-in-desktopg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/DesktopGateScreen.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "The QR code target URL is derived from window.location.origin + import.meta.env.BASE_URL instead of a hardcoded production domain string"
    - "No module-level PRODUCTION_URL constant remains in DesktopGateScreen.tsx"
  artifacts:
    - src/screens/DesktopGateScreen.tsx
  key_links:
    - "QRCode.toDataURL call reads the dynamically computed URL, keeping it tied to vite.config.ts's base: '/team-08-app/' via import.meta.env.BASE_URL (no manual duplication)"
---

<objective>
Replace the hardcoded `PRODUCTION_URL` constant in `DesktopGateScreen.tsx` with a dynamically derived URL (`window.location.origin + import.meta.env.BASE_URL`), so the QR code always points at wherever the app is actually deployed instead of a single hardcoded production domain.

Purpose: The hardcoded URL duplicates the base path already defined in `vite.config.ts` (`base: '/team-08-app/'`) with no compiler-enforced link between them — fragile if the deploy domain or base path ever changes, and wrong on forks/staging. Flagged during code review of PR #82.
Output: `src/screens/DesktopGateScreen.tsx` with the QR target computed dynamically at the point `QRCode.toDataURL` is called; no behavior change to the rest of the component.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@src/screens/DesktopGateScreen.tsx
@vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded PRODUCTION_URL with dynamic origin + BASE_URL</name>
  <files>src/screens/DesktopGateScreen.tsx</files>
  <action>
    In src/screens/DesktopGateScreen.tsx, delete the module-level constant `const PRODUCTION_URL = 'https://vibecode.tours/team-08-app/'` (line 9).

    Inside the component's `useEffect` (the one calling `QRCode.toDataURL`), compute the QR target as `window.location.origin + import.meta.env.BASE_URL` and pass that expression as the first argument to `QRCode.toDataURL` in place of the removed `PRODUCTION_URL` reference. Either inline the expression directly in the call or assign it to a local `const` (e.g. `qrTargetUrl`) declared at the top of the effect body before the `QRCode.toDataURL` call — either form satisfies the requirement as long as no module-level hardcoded URL constant remains and the value is computed at effect-run time (not at module load time), so it reflects the live `window.location.origin`.

    Do not special-case dev vs. production — resolving to `localhost` during `npm run dev` is expected and accepted per the task description. Do not introduce any new environment variable, config file change, or conditional branching for dev/prod. Do not modify vite.config.ts. Leave all other logic in the file (cancelled-flag cleanup, catch handler, JSX) untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.app.json && npx eslint src/screens/DesktopGateScreen.tsx</automated>
  </verify>
  <done>
    - `PRODUCTION_URL` no longer appears anywhere in src/screens/DesktopGateScreen.tsx
    - `QRCode.toDataURL` is called with an expression built from `window.location.origin` and `import.meta.env.BASE_URL`
    - `npx tsc --noEmit -p tsconfig.app.json` passes with no errors
    - `npx eslint src/screens/DesktopGateScreen.tsx` passes with no errors
  </done>
</task>

</tasks>

<verification>
Run the full project verification suite:
1. `npm run lint` — must pass with no errors
2. `npm run test` — must pass (no existing test exercises DesktopGateScreen's QR URL directly, but the full suite must remain green)
3. `npm run build` — must complete successfully (validates TypeScript + Vite production build, including `import.meta.env.BASE_URL` usage compiles correctly)

Manual sanity check (optional, not required for automated pass): grep confirms no hardcoded `vibecode.tours` string remains in src/screens/DesktopGateScreen.tsx.
</verification>

<success_criteria>
- `src/screens/DesktopGateScreen.tsx` contains no hardcoded production URL string
- QR code target is computed from `window.location.origin + import.meta.env.BASE_URL` at the point of use
- `npm run lint && npm run test && npm run build` all pass
</success_criteria>

<output>
Create `.planning/quick/260718-6dk-fix-hardcoded-production-url-in-desktopg/260718-6dk-SUMMARY.md` when done
</output>
